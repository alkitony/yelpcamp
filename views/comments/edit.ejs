<% include ../partials/header %>

<div class="container">
   <div class="well well-sm">
      <div class="row">
         <h2 class="formTitle">Update comments to: <%= campground.name %> </h2>
         <div class="formStyle">
            <form class="" action="/campgrounds/<%= campground._id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
               <div class="form-group">
                  <input class="form-control" type=text name=comment[comment] value="<%= comment.comment %>">
               </div>
               <div class="form-group">
                  <button class="btn btn-lg btn-primary btn-block">Submit!!</button>
               </div>
            </form>
            <a href="/campgrounds/<%= campground._id %>">Go Back</a>
         </div>
      </div>
   </div>
</div>

<% include ../partials/footer %>